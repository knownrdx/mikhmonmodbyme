version: "3.8"
services:
  mikhmon:
    build: .
    container_name: mikhmon
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - mikhmon-data:/var/www/html/data

volumes:
  mikhmon-data:
  
